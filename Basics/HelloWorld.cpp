// This line includes the iostream library, which provides functionality for input/output operations
#include <iostream>

int main() { // This is the main function, which is the entry point of the program
    // This line prints "Hello, World!" to the console, followed by a newline character
    std::cout << "Hello, World!" << std::endl;
    // The << operator is commonly used with output streams, such as std::cout, to print data to the console. This is also known as the stream insertion operator.
    // The << operator is also used for performing bitwise left shift operations on integers. This shifts the bits of the left-hand operand to the left by the number of positions specified by the right-hand operand, filling the vacated bits with zeros.


    // This line returns 0 to the operating system, indicating successful program execution
    return 0;
}
